import Grid from "@mui/material/Grid";

import { styled } from "@mui/material/styles";
import { grey } from "@mui/material/colors";

const StyledBooksContainer = styled(Grid)(() => ({
  padding: 100,
  backgroundColor: grey[500]
}));

export { StyledBooksContainer };
